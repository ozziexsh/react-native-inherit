import { TouchableOpacity as Component } from 'react-native';
import { createInheritedComponent } from './base';

const TouchableOpacity = createInheritedComponent(Component);

export default TouchableOpacity;
